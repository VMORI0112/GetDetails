import React from 'react';
import styles from './Chat.module.css';

const Chat = () => {
    return (
        <section className={styles.section}>
            chat page
        </section>
    );
};

export default Chat;